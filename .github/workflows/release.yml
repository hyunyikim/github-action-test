name: Release Tag
on:
  pull_request:
    branches:
      - main
jobs:
  if_merged:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: PR merge
        run: echo The PR was merged
